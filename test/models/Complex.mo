within ;
record Complex "Dummy Complex operator (not implemented yet)"

  replaceable Real re "Real part of complex number" annotation(Dialog);
  replaceable Real im=0 "Imaginary part of complex number" annotation(Dialog);

end Complex;
